# Local state + secrets — never commit these
state.json
config.local.json
sites.local.json

# wog auto-buyer — secrets, order ledger, and checkout recon dumps
.env
bought.json
checkout_dump/
checkout_*.html
wog_app.min.js

# Telegram controller runtime state (update offset + pending buy targets)
telegram_state.json

# Editor / tooling
.claude/

# Python
__pycache__/
*.pyc
.venv/
venv/
